# Netlify Project ID: e766f064-9174-47e8-80d3-3a8c08542c9a
# Verificare la sincronizzazione nella dashboard Netlify:
# https://app.netlify.com/sites/[SITE_NAME]/deploys

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Redirect tutte le route a index.html per React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Headers di sicurezza
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Ottimizzazioni per i file statici
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable" 